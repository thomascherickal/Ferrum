[package]
name    = "slm_cli"
description = "Command-line trainer and generator for ferrum_core causal transformer Small Language Models (int8 QAT, on-disk weight caching)."
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "train_transformer"
path = "src/main.rs"

[dependencies]
ferrum_core = { path = "../ferrum_core" }
