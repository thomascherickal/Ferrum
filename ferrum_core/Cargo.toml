[package]
name        = "ferrum_core"
description = "Zero-dependency edge Transformer & MLP engine — CPU-only, no_std-compatible, WASM-ready. Hand-build causal Transformers with pure Rust."
version.workspace = true
edition.workspace = true
license.workspace = true
readme      = "README.md"
keywords    = ["transformer", "edge-ai", "neural-network", "wasm", "no-std"]
categories  = ["science", "no-std::no-alloc", "wasm", "embedded"]

[dependencies]
# intentionally empty — std only, zero external dependencies
