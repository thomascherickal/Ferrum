# Build artefacts
target/

# Raw download files (rebuilt by scripts/download_datasets.sh)
*_raw.csv
*_raw.data
*_raw.txt
iris_wasm_old_lib.rs

# Old iris-specific WASM (replaced by tabular_wasm)
web/pkg/iris_wasm.js
web/pkg/iris_wasm_bg.wasm
web/pkg/.gitignore

# Stale web-root artefacts from v1
web/app.js
web/style.css
web/model.bin

# Tabular demo models generated by scripts/train_all.sh (deploy CI rebuilds
# them from datasets/tabular/). The three SLM demos (ambient_poet,
# brand_alchemist, shell_oracle) are deliberately committed and NOT listed here.
web/datasets/cancer/
web/datasets/diabetes/
web/datasets/heart/
web/datasets/housing/
web/datasets/iris/
web/datasets/mpg/
web/datasets/penguins/
web/datasets/seeds/
web/datasets/titanic/
web/datasets/wine/

# Generated WASM (rebuilt by CI — committed for local dev convenience)
# Remove these lines if you prefer to commit the WASM artefacts:
# web/pkg/tabular_wasm.js
# web/pkg/tabular_wasm_bg.wasm

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/settings.json
